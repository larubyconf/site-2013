=render :partial => 'admin/admin_navigation'
.row
  .span12
    .well
      =link_to 'Add News item', new_admin_news_item_path, :class => 'btn pull-right btn-primary'
      %legend
        News Items
      %table#news-items.table.table-striped
        %thead
          %tr
            %th
              Headline
            %th
              Body
            %th
              Display Date
            %th
              Expire Date
            %th
              By
            %th
              Actions
        %tbody
          = render :partial => 'news_item', :collection => @news_items