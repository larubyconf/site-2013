%tr
  %td
    =display_level sponsor.level
  %td
    =sponsor.name
  %td
    =sponsor.sponsored_at
  %td.center
    =link_to edit_admin_sponsor_path(sponsor) do
      %i.icon-pencil
    =link_to admin_sponsor_path(sponsor), :method => :delete do
      %i.icon-trash