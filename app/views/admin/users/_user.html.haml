%tr
  %td
    = link_to user.email, admin_user_path(user)
  %td
    =user.authentications.count
  %td.center
    =link_to edit_admin_user_path(user) do
      %i.icon-pencil
    =link_to admin_user_path(user), :method => :delete, :rel => 'nofollow' do
      %i.icon-trash
